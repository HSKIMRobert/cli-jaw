FAIL
[MEDIUM] ARCH-A1 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:§6 — Section 6 mandates opt-in startup evidence (`\x1b[?1000h\x1b[?1006h`) but pins no test file or source regex beyond existing `viewport.scrollBy(...)`; an implementation can pass the listed unit suite while regressing the startup enable path — add a mandatory source assert (e.g. in `tests/unit/tui-copy-friendly-mouse.test.ts` or `tests/unit/tui-fullscreen-source-contract.test.ts`) matching `isMouseTrackingEnabled(ctx)` at the `screen.enableMouse()` call site.
[MEDIUM] ARCH-A2 tests/unit/tui-chat-launch-scope.test.ts:29 — The embedded fullscreen startup snippet still expects `ctx.tuiConfig['mouseTracking'] === true` while the plan mandates `isMouseTrackingEnabled(ctx)` at startup; this file is in the pinned verification command and the plan cites it for mouse startup contract — update the snippet (or assert against live `fullscreen-mode.ts`) when applying §2.
[LOW] ARCH-A3 bin/commands/tui/fullscreen-mode.ts:500-504 — Verified today: SGR parsing is unconditional and `handleMouseEvent` can call `screen.enableMouse()` at :215/:226; plan §2–§3 correctly targets this, but implementation must land atomically with `tests/unit/tui-fullscreen-source-contract.test.ts:50` (`!== false`) removal or the focused suite fails on contradiction — same commit for chat, fullscreen-mode, and both source-contract tests.

Delta re-audit (prior Architect lens): unconditional SGR + leaked enable path (stage-01-architect.md finding 3) — accepted in plan via `mouseTrackingEnabled && isMouseSequence(incoming)` and `handleMouseEvent(..., mouseTrackingEnabled)`; chat default `mouseTracking: true` (plan root cause #1) — accepted via omission at `bin/commands/chat.ts:107` + `tui-chat-launch-scope` assert; press/timer selection tradeoff — waived per non-targets; 3J launch clear — out of scope; PTY/smoke coverage — deferred with evidence.

Single point most likely to break first: shipping `bin/commands/tui/fullscreen-mode.ts` stdin gating and `bin/commands/chat.ts` default change without updating `tests/unit/tui-fullscreen-source-contract.test.ts:50`, leaving `!== false` while other tests expect `=== true`/helper gates — focused `npx tsx --test` fails immediately; if tests are skipped, leaving line :500 ungated preserves leaked-SGR re-enable of mouse reporting in default native mode.
