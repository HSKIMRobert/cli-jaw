FAIL
[MEDIUM] ARCH-A4 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:253 — §5 mandates `assert.match(source, /process\.stdout\.write\('\\x1b\[\?1000h\\x1b\[\?1006h'\)/)` while the same section restricts `source` to `bin/commands/tui/fullscreen-mode.ts` only; DECSET bytes live in `src/cli/tui/render/frame.ts:364` (`enableMouse()`), so the pinned test update cannot pass as written — read `frame.ts` in that test (or drop the write regex and assert startup `if (isMouseTrackingEnabled(ctx)) screen.enableMouse()` only).
[MEDIUM] ARCH-A5 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:271-279 — A-r2 accepts ARCH-A2 at :421 (“startup snippet must align with `isMouseTrackingEnabled(ctx)`”) but §5.5 only adds `chatSource` `mouseTracking: true` negation; repo `tests/unit/tui-chat-launch-scope.test.ts:29` still embeds `ctx.tuiConfig['mouseTracking'] === true` and that file is in the verification command — add an explicit §5.5 diff/assert for the startup snippet (or `assert.match(fullscreenSource, /if \(isMouseTrackingEnabled\(ctx\)\) screen\.enableMouse\(\)/)`).

Prior finding map (a-r1 → a-r2 plan evidence):
- ARCH-A1 (opt-in startup/DECSET test ownership): accepted in plan §5/:414 and synthesis; startup gate regex is specified; DECSET evidence remains integration-broken per ARCH-A4 until file scope is fixed.
- ARCH-A2 (launch-scope startup snippet): accepted at :421/:440 but procedural gap per ARCH-A5 — not fully resolved for guess-free implementation.
- ARCH-A3 (atomic prod+test commit): accepted at §4/:241, :422, and exact patch target list — sufficient constraint.

Single point most likely to break first: implementing §5 `tui-copy-friendly-mouse.test.ts` with `fullscreen-mode.ts` as the sole `source` and the mandated `process.stdout.write(?1000h?1006h)` regex — the focused unit run fails immediately (no match in fullscreen-mode) even if production gating is correct.
