FAIL
[high] PLANNER-A1 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:275-287,406-412 — Acceptance row "Opt-in app wheel mode still works" and §6 require startup `\x1b[?1000h\x1b[?1006h` and SGR parsing, but no named test file, test title, or concrete source-level regex/assert snippet (unlike §4–§5); repo has only `viewport.scrollBy` source match in `tests/unit/tui-copy-friendly-mouse.test.ts`, not DECSET enable evidence — add §6 patch target (e.g. `tui-copy-friendly-mouse.test.ts` or `tui-screen-lifecycle.test.ts`) with exact `assert.match` patterns for `enableMouse`/`?1000h` when `mouseTracking === true`.
[high] PLANNER-A2 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:252-256,265-271 — §5 mandatory bullets require reading `bin/commands/chat.ts` and denying `mouseTracking: true`, while §5.5 assigns the same evidence to `tui-chat-launch-scope.test.ts` only; §5 code block omits chat assertions — pick one owning test file (or both with explicit non-duplication rule) and list the exact test name(s) to edit.
[medium] PLANNER-A3 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:118,287 — Non-targets/§6 require documenting the opt-in press `disableMouse()` + 2s `enableMouse()` selection tradeoff, but no work item names where that documentation lives (Risks vs user-facing doc vs code comment) — add a single mandated doc location and one checkable sentence to include.
[medium] PLANNER-A4 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:8-15,406-410 — Goal requires native drag selection and terminal scrollback without config change; Acceptance Matrix has no independently checkable row for those (only default-off mouse reporting proxy) — add explicit acceptance evidence (named smoke/manual step or harness assertion) or mark as out-of-scope for this cycle with reason.
[medium] PLANNER-A5 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:156-161,185-194 — §2 and §3 both show `+const mouseTrackingEnabled = isMouseTrackingEnabled(ctx);` in separate diffs without stating single declaration scope in the stdin handler — state "declare once per input dispatch" (or show one combined diff) so implementers do not duplicate or hoist incorrectly.
[low] PLANNER-A6 devlog/_plan/260614_interactive_mode_full_port/333_cycle45_5_jawcode_native_scrollback_mouse_contract.md:109-111,414 — Exact Patch Targets item 6 lists `tui-screen-lifecycle.test.ts` and `tui-fullscreen-frame-harness.test.ts` without "no code changes, verification only" vs required edits — label each as regression-only run or enumerate required test changes to avoid scope churn.
[low] PLANNER-A7 Critic Round 2 Synthesis G1–G5 — G1–G5 are recorded accepted; residual Critic F7 (press/timer tradeoff in acceptance) is only partially closed via non-targets, not matrix — either add matrix note or waive F7 with explicit "source-only cycle" rationale in synthesis packet for a-r1.

Most likely misread: §6 "Add or adjust a focused test" for opt-in mode — implementer will only keep/extend the existing `viewport.scrollBy` source assert and treat startup `\x1b[?1000h\x1b[?1006h` as satisfied by narrative, not a mandatory named test change.
