Summary: Ghostty scroll-up blocked because commit lane often no-ops when lastFillRows is 0; user only gets virtual viewport page-up. Architectural Status: BLOCK. Code Review Recommendation: REQUEST CHANGES. Root cause: normalizeFrameRows zeros fillRows when VIEWPORT_FILL is not at index 0 or frame overflows height; commitLines requires liveZoneTop greater than 0. jawcode keeps fill at index 0 and overflow sticky gap. Idle scheduler is event-driven in both; CSI 2026 does not block scrollback; DECSTBM reset via ESC bracket r after commit.
